
from setuptools import setup

# suggest to install by 
# python setup.py build develop --user

setup(name='qd_delf',
      version='0.1',
      description='DeLF related pipeline, includes training, extracting',
      packages=['qd_delf'],
     )
